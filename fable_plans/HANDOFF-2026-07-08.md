# Fable Plans Handoff - 2026-07-08

Written after an independent review of the whole `fable_plans/` set that
spot-checked every recorded census and ticked checkbox against source. This
supersedes `HANDOFF-2026-07-07.md`.

## Branch state — read before committing anything

- `nightrunner-7th-july` was **merged to `main` via PR #106**. Do NOT push
  new commits to it. Start (or use your session's assigned) fresh feature
  branch off the latest `main`.
- Worktree hygiene rules from `AGENTS.md` apply unchanged: run
  `git status --short --branch` before edits, treat pre-existing dirty files
  as user-owned, never force-push or rewrite history.

## Review outcome (what changed in this folder on 2026-07-08)

The ticked checkboxes in every progress file were verified against source and
are genuine — trust them. The following corrections were applied; read them
before working the affected plans:

1. **02-progress CENSUS CORRECTION (important).** The recorded `Gfx()` census
   (25 hits, "0 orphans") was wrong: it used `rg`, which respects
   `.gitignore`, and `.gitignore`'s `Debug/` pattern hides the TRACKED
   directory `SkullbonezSource/Physics/Debug/`. True count is 43 text hits;
   19 of them are real backend calls in the physics debug visualizers. A new
   box **G2b** covers converting them. The enforced checker ratchet was never
   affected (it enumerates gitignore-blind) — only the document was wrong.
2. **Census rule everywhere:** inventories/censuses must use `git ls-files` +
   `grep -rn`, never bare `rg`. Added to the README and the affected
   progress-file preambles.
3. **Ratchet rule everywhere:** never raise a checker budget or add an
   allowlist row to get green. Budgets only go down; a red ratchet means stop
   and report.
4. **03-progress phase 3** is now explicitly HUMAN-AWAKE with a new design
   gate (P3.0) for the nested worker-pool hazard, and its PR gate now requires
   3 consecutive `validate_physics` runs (a threading race can pass one
   byte-exact run).
5. **05-progress P2.3** now requires a forced-failure (sabotage) check per
   converted probe cluster, not one in total — the silent failure mode is a
   caller that forgets to propagate the new `SbResult` and turns "probe
   failure ends the run" into a log line while all green-path proofs pass.
6. **08-progress P3.2** gate corrected to include `validate_full`
   (`Runtime/Replay/*` files route there per the AGENTS.md file map).
7. **Proof scripts** must live in `SkullbonezData/interaction/` from now on.
   The 08 P2.x/P3.1 evidence cites scripts under `Agentic/Temp/`, which is
   gitignored — those proofs are not reproducible from the repo.
8. **Drift notes** added: `PhysicsWorld.cpp` grew to 3,947 lines (plan 04's
   mega-file problem is worsening in physics); throw census is 355 tokens in
   48 files / 30 catches in 13 files; plan-06 inventory line numbers drifted a
   few lines (anchor by member name); the vcxproj toolset is v145 (AGENTS.md's
   "v143" is the stale document).

## Recommended next work (unsupervised-safe, value order)

1. `08-demo-director-progress.md` P3.2 (reveal rate), then phase 4 and
   closure. Likely symbols: `REPLAY_PREDICTION_REVEAL_SECONDS_PER_SECOND`,
   `RunReplayPredictionState`, `ReplayPredictionRevealFrameIndex`,
   `RunReplayTools.cpp`, `DemoDirectorPlayback::Tick`. Keep reveal-rate work
   presentation-side; gates per the corrected P3.2 box.
2. `09-consequence-look-progress.md` from phase 0 — its plan-03 dependency
   (private prediction engine) is met; plan-03 phase 3 is NOT required.
3. `06-stable-identity-progress.md` from I1 — not started, inventory
   pre-verified on 2026-07-06 and drift-annotated on 2026-07-08.
4. `02-global-service-retirement-progress.md` G2b (the 19 Physics/Debug
   `Gfx()` conversions), then `05` phases 2-3.

## Do NOT attempt unsupervised

- Plan-07 Cluster A1/B1 design docs and RUN-015 (marked human-awake there).
- Plan-03 phase 3 (worker-job stepping) — new P3.0 design gate first.
- Plan-04's pack-shrink history rewrite (user-owned decision, recorded in
  `Agentic/SessionState.md`).
- Plan-02 G3 / SVC-001/002 endgame deletions (conditions not yet met; the
  grep-based caller count must reach the startup allowlist first).

## Process reminders

- Follow the startup contract in `AGENTS.md` before editing.
- Work from the progress files; plan files are rationale.
- Documentation-only changes require no repository validation.
- Source-bearing changes require the touched-file comment-quality audit.
- Tick a checkbox only with the named evidence pasted under it; `[B]` +
  reason if blocked twice, then stop that phase — do not improvise around it.
