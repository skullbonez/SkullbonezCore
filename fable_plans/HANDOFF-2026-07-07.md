# Fable Plans Handoff - 2026-07-07

## User Direction

The user stopped the current long-context implementation run and asked for a
handoff. The next agent should continue in a fresh context.

Important explicit instruction from the user:

> Complete ALL FABLE PLANS in addition to this blocker cleanup.

That means the blocker-remediation cleanup is not the full finish line. The
remaining work in `fable_plans/` should be executed to completion, with commits,
validation, and pushed branch state as required by `AGENTS.md` and the
orchestrator skill.

## Current Branch

- Branch: `nightrunner-7th-july`
- Repo: `C:\SkullbonezCore`
- Use the normal startup contract before continuing.
- CodeGraph exists and was current during this run.

## Completed And Pushed Before This Handoff

Commit pushed:

- `e04d7fec fix: classify lock-order validator singleton`

That commit resolved the fable remediation quick win for SVC-034:

- Classified `LockOrderValidator::Instance` as frozen diagnostics singleton
  debt.
- Removed it from the counted global-service compatibility allowlist.
- Lowered the runtime-boundary global-service census budget from 157 to 152.
- Marked SVC-034 done in the authoritative plan CSV and removed it from the
  overnight blocker ledger.
- Validation passed:
  - `python tools\check_runtime_boundaries.py --self-test`
  - `python tools\check_runtime_boundaries.py`
  - `tools\validate_fast.bat`

## Current Uncommitted Work

Implementation for `fable_plans/01-unit-test-pyramid-progress.md` phase 0 is
present in the worktree and was validated, but was not committed before the user
asked to stop.

Implemented:

- Vendored doctest v2.4.12 at `ThirdPtySource/doctest/doctest.h`.
- Added `ThirdPtySource/README.md`.
- Added `SkullbonezTests/TestMain.cpp` and `SkullbonezTests/TestSmoke.cpp`.
- Added `SKULLBONEZ_TESTS.vcxproj` and `SKULLBONEZ_TESTS.vcxproj.filters`.
- Added the test project to `SKULLBONEZ_CORE.sln`.
- Added `tools\validate_tests.bat`.
- Wired `tools\validate_fast.bat` to run `validate_tests` as step 5.
- Added partial-project support to `tools\validate_project_filters.py`.
- Updated `AGENTS.md`, `tools/README.md`, `Agentic/SessionState.md`,
  `fable_plans/01-unit-test-pyramid-progress.md`, and
  `fable_plans/07-blocker-remediation-plan.md`.
- Added report `Agentic/Reports/2026-07-07/fable-01-phase-0-harness.md`.

Path deviation to preserve:

- The original fable checklist suggested `SkullbonezSource/ThirdParty/`.
- This run used existing repo convention `ThirdPtySource/doctest/` instead,
  because `tools/validate_project_filters.py` treats files under
  `SkullbonezSource` as production project inventory.

Validation already passed for the uncommitted phase-0 harness:

```text
tools\validate_tests.bat
VALIDATE_TESTS: ALL PASSED
elapsed: 5.192s
0 warnings, 0 errors
doctest 2.4.12: 1 test case passed, 1 assertion passed

tools\validate_fast.bat
VALIDATE_FAST: ALL PASSED
elapsed: 34.166s
format/project filters/runtime boundaries/Profile+Debug builds/unit tests green
0 warnings, 0 errors
```

Logs:

- `Agentic/Reports/2026-07-07/logs/fable-01-validate-tests.log`
- `Agentic/Reports/2026-07-07/logs/fable-01-validate-fast.log`

## Next Agent Immediate Steps

1. Run the startup contract from `AGENTS.md`.
2. Inspect `git status --short --branch` and treat the current dirty worktree
   as in-progress agent work from this handoff.
3. Review the uncommitted fable-01 phase 0 diff.
4. If the diff still looks right, commit and push it with explicit validation
   notes from the two logs above.
5. Continue the fable blocker remediation plan, then continue until all fable
   plans are complete.

## Suggested Next Work Order

From `fable_plans/07-blocker-remediation-plan.md`:

1. Finish committing/pushing fable-01 phase 0 harness bootstrap.
2. Continue the quick-win path:
   - PHYS-035 prediction isolation via `fable_plans/03-prediction-isolated-world-progress.md`.
3. Then continue through the remaining fable plans, not just the blocker list.

Do not revive the old retired orchestrator JSON/Python path. Use
`Agentic/Skills/orchestrator/SKILL.md` and normal repo validation discipline.
