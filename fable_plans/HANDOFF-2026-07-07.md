# Fable Plans Handoff - 2026-07-07

## User Direction

Continue from `main` on a new night-runner branch without throwing away existing
work. The user first asked for fable plans 1-7, then approved continuing into
fable-08 and fable-09. For fable-09, the user specifically wanted replay/debug
lines to become smooth and anti-aliased rather than jaggy, plus shader-style
options for glow/emphasis in the butterfly-effect demo.

## Current Branch

- Branch: `nightrunner-7th-july-complete-fables`
- Repo: `C:\Users\sesch\.codex\worktrees\0595\SkullbonezCore`
- Branch base: `origin/nightrunner-7th-july-plans-8-9` at
  `94f3eff0 fix: convert replay probes to results`.
- Current slice: fable-06 C1 mouse-pickup redundant-row sub-slice plus a
  companion fable-05 missing-camera parser throw removal are complete; deeper
  fable-05 loader/asset and DX12 conversions plus remaining fable-06 C1
  selection/drag-group work remain open.
- CodeGraph is not initialized in this worktree; use normal targeted source
  reads unless a future worker initializes an index.

## Current Clean Exit Point

Fable-01, fable-02, fable-08, and fable-09 are complete. Fable-04 L1-L4 are
complete and validated: `SKULLBONEZ_MATHS.vcxproj` now owns the 12 Maths files,
the app and tests link it through native project references, and the
project-filter checker validates app plus extracted production libraries as one
source-ownership set. Fable-04 C1-C5 are complete:
`Physics/PhysicsTimestep.h` owns the fixed-step constants and
`GameObjects/SceneCapacity.h` owns compile-time scene/camera/texture capacity
constants; `Assets/AssetKeys.h` owns `HashStr`, the built-in texture hash
constants, and the camera hash constants; `Runtime/WindowConstants.h` owns the
window labels and data-root string; `Common.h` includes all four during the
aliasing period and now records each alias deletion condition. C1-C4 were
source gates; C5 was comment/docs-only and required no repository validation.
Fable-03 P3.0 safety prep is complete: build-prefix publication now routes
through `RunReplayPredictionState` helpers and leaves the actual async
worker-job implementation deferred. Fable-06 Phase 1 and Phase 2 R1/R2/R3 are
complete and validated.
Fable-05
P2.1-P2.3 are complete: RunFrame probe throw clusters are inventoried by
function, classified as Debug-only CLI probe surfaces rather than direct
interaction automation actions, converted to `SbResult`, and proven through
interaction proofs plus a forced CLI probe failure. Fable-05 P3 SpatialGrid is
also complete: 13 SpatialGrid throw sites and 6 PhysicsWorld throw sites now
use `SB_FATAL`, the throw ratchet dropped to 275, and `validate_fast` plus
`validate_physics` passed for both one-file slices. Fable-05 P3
GameModelCollection classification is complete, its pure post-append topology
  failures now use `SB_FATAL`, legacy object-follow camera reads now use
  `TryGetModelPosition`, and append capacity/missing scene identity/group
  descriptor failures now return `GameModelAppendResult`/`SbResult`; the throw
  ratchet is 266. The P4.1 scene/style entry boundary is also complete:
  `TestScene::TryLoadFromFile` and `TryLoadStyleFromFile` return `SbResult`,
  runtime scene/style callers consume those results, and startup/load-only scene
  failures now exit nonzero instead of continuing after `scene_load_failed`.
  The direct missing-camera parser throw now routes through path-rich
  `Fail(path, detail)`, and the throw ratchet is 265. Deeper parser throw
  removal plus Terrain, TextureCollection, AssetSystem, and ConvexHullShape
  loader clusters remain for Phase 4.
Fable-06 C1 has a partial stable-identity checkpoint: mouse pickup no longer
stores a redundant `modelIndex` next to `PhysicsBodyHandle`; live pickup
physics and overlay drawing resolve/revalidate through the body handle. C1
remains open for selection hints and drag-group rows, and the stored model-index
ratchet is now 27.
Fable-09 Phase 0 discovery, Phase 1 consequence grade,
Phase 2 smooth glowing replay ribbons, Phase 3 two-tone baseline, Phase 4
divergence counter, and Closure Z1/Z2/Z3 are complete. Earlier fable plans
still have open work; see the progress table below.

What this fable-09 slice did:

- Adds a replay-only smooth ribbon draw path:
  `IRenderCommandContext::DrawReplayRibbons`,
  `RenderBackendDX12::DrawReplayRibbons`, and
  `SkullbonezData/shaders/replay_ribbon.hlsl`.
- Registers `shader.replay_ribbon` in `AssetSystem` and
  `tools/shader_contracts.json`.
- Changes replay causal trails, retained causal trails, and entry/rest/horizon
  marker outlines to emit bounded camera-facing ribbon geometry with separate
  core/glow style layers.
- Leaves generic debug overlays on the old `DrawLinesColored`/`grid_line.hlsl`
  path so non-demo debug UI is unchanged.
- Adds a frame-local consequence grade owned by `RuntimeRenderer`, requested
  by replay prediction enablement and eased over roughly one second.
- Adds `SkullbonezData/styles/consequence.style.json` as a dark/cool authored
  style alternative for Director phases.
- Updates fable-09 plan/progress docs with discovery findings and evidence.
- Carries forward the Phase 1/2 touched-file comment audit for 12
  source-bearing files and completes the Phase 3/4 touched-file comment audit
  for 11 source-bearing files, both with 0 deferred.
- Adds the retained two-tone baseline: old committed futures are captured into
  bounded replay-reserved baseline root/body-pose buffers, rendered cold in
  cyan with smooth replay ribbons and low-alpha ghost meshes, then compared
  against the warm rebuilt future.
- Adds a clean-demo divergence counter in `RunUiTextPass` that displays the
  summed rest-pose separation when `--hide-top-text` is active.
- Adds deterministic interaction proof actions for a compact prediction horizon
  and a proof-only path-target velocity nudge. The nudge now fails before
  mutating if a completed prediction is not available to baseline.
- Fixes the reviewed runtime-allocation risk by pre-reserving
  `REPLAY_PREDICTION_GHOST_REQUEST_CAPACITY` during `ReplayRuntime`
  construction; render-time ghost request building now fails closed instead of
  growing the vector.

## Evidence

Interaction proofs on the final build:

- `prediction_ragdoll_wall_200_predict` proof passed:
  `TestOutput\interaction\fable09_prediction_ragdoll_report.json` has
  `ok=true`, `predictionPathVisible=true`,
  `liveSolverHashStableAcrossPrediction=true`, target
  `prediction_striker_ball`, and screenshot
  `TestOutput\interaction\prediction_ragdoll_wall_200_predict.bmp`.
- Butterfly Director proof passed:
  `TestOutput\interaction\fable09_butterfly_ribbon_report.json` has `ok=true`
  and reached phases `root-sighting`, `chain-bloom`, and `settle-rest`.
  Screenshots:
  `TestOutput\interaction\butterfly_phase_0_root_sighting.bmp`,
  `TestOutput\interaction\butterfly_phase_1_chain_bloom.bmp`,
  `TestOutput\interaction\butterfly_phase_2_settle_rest.bmp`.
- Baseline/divergence proof passed:
  `TestOutput\interaction\fable09_baseline_divergence_report.json` has
  `ok=true`, `predictionBaselineVisible=true`, 181 cold root points, 174
  baseline body poses, `predictionDivergenceValid=true`, and divergence
  `457.13067626953125`. Screenshot:
  `TestOutput\interaction\fable09_baseline_divergence.bmp`.
- Current `prediction_ragdoll_wall_200_predict` proof rerun passed:
  `TestOutput\interaction\fable09_prediction_ragdoll_current_report.json` has
  `ok=true`, `predictionPathVisible=true`, and
  `liveSolverHashStableAcrossPrediction=true`.
- Fable-05 P2.3 proofs passed on 2026-07-08:
  `memory_overlay_f6_toggle_report.json`, `replay_branch_restore_live_edge_report.json`,
  and `prediction_ragdoll_wall_200_predict_report.json` all have `ok=true`.
  The prediction proof also has `predictionPathVisible=true` and
  `liveSolverHashStableAcrossPrediction=true`.
- Fable-05 forced CLI probe failure passed: a missing
  `--replay-restore-file-probe` artifact returned process exit 1 with
  `[replay] Probe failed: replay restore file probe failed to load v2 solver checkpoints`
  and no `fatal_exception`.

Validation:

- Fable-02 Gfx deletion closure gates passed on 2026-07-08:
  `tools\validate_fast.bat` passed in 00:00:50.6759731;
  `tools\validate_dx12_renderer.bat` passed in 00:00:20.1867231 with DX12
  validation errors 0 and screenshots matching committed baselines;
  `tools\validate_full.bat` passed in 00:00:43.8587184 with project filters and
  runtime boundaries at 0 errors, Profile/Debug builds at 0 warnings/errors,
  DX12 errors 0, DX12 screenshots matching baselines, and
  `physics_regression_solver.csv` byte-exact.
- `tools\validate_fast.bat` passed in 00:00:51.8488989:
  formatting, project filters, staged-size, runtime boundaries, unit tests, and
  Profile/Debug builds passed with 0 warnings/errors.
- `tools\validate_dx12_renderer.bat` passed in 00:00:19.9648831:
  DX12 InfoQueue reported 0 validation errors and DX12 screenshots matched
  committed baselines.
- `tools\validate_full.bat` passed in 00:00:42.6519534:
  runtime boundaries passed, Profile/Debug builds had 0 warnings/errors, DX12
  validation errors were 0, screenshots matched baselines, and
  `physics_regression_solver.csv` matched byte-exactly.
- `python tools\validate_shaders.py --json-out TestOutput\validation\shaders\fable09-summary.json`
  passed in 00:00:00.1876283 with 0 errors and 11 existing manifest-coverage
  warnings.
- Current Phase 3/4 gates after the allocation-review fix:
  `tools\validate_dx12_renderer.bat` passed in 00:00:24.9698923 with DX12
  validation errors 0 and screenshots matching committed baselines.
- `tools\validate_full.bat` passed in 00:00:44.6889136 with project filters and
  runtime boundaries at 0 errors, Profile/Debug builds at 0 warnings/errors,
  DX12 errors 0, DX12 screenshots matching baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-06 Phase 1 gates:
  `tools\validate_fast.bat` passed in 00:00:34.4710073 with formatting, project
  filters, staged-size, runtime boundaries, unit tests, and Profile/Debug builds
  all passing at 0 warnings/errors; post-gate
  `python tools\check_runtime_boundaries.py --max-errors 20` passed in
  00:00:17.2728378 with 0 errors.
- Fable-06 Phase 2 R1/R2/R3 gates:
  `Physics::ModelRowHint`, `PhysicsBodyStore::ResolveModelRow`, and the
  replay-id `ModelRowHint&` resolver overload are in place; retained replay
  marker/camera/prediction-begin lookups now repair or invalidate their
  row-hint caches through the resolver. `TestPhysicsHandles.cpp` covers stale
  handle invalidation and moved-row hint self-heal. Euclid provided the
  read-only source map; touched-file comment audit inspected 6 source-bearing
  files with 0 deferred. `tools\validate_fast.bat` passed in 00:00:52.8044 with
  formatting, project filters, staged-size, runtime boundaries, doctests
  `45 | 45 passed` / `582 | 582 passed`, and Profile/Debug builds at 0
  warnings/errors. Post-gate
  `python tools\check_runtime_boundaries.py --max-errors 20` passed in
  00:00:17.8207 with runtime boundaries at 0 errors.
- Fable-05 P2.2/P2.3 gates:
  `python tools\check_runtime_boundaries.py --self-test` passed; `python
  tools\check_runtime_boundaries.py --max-errors 20` passed with 0 errors and
  `MAX_SOURCE_THROW_TOKENS = 294`; `tools\validate_fast.bat` passed in 67.232s;
  `tools\validate_full.bat` passed in 45.705s with DX12 validation errors 0,
  DX12 screenshots matching committed baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-05 P3 SpatialGrid gates:
  `python tools\check_runtime_boundaries.py --self-test` passed; `python
  tools\check_runtime_boundaries.py --max-errors 20` passed with 0 errors and
  `MAX_SOURCE_THROW_TOKENS = 281`; focused `SKULLBONEZ_TESTS.vcxproj`
  project-filter validation passed with 48 project items, 48 filter items, and
  0 errors; `tools\validate_fast.bat` passed in 00:00:38.1202317; and
  `tools\validate_physics.bat` passed in 00:00:15.2232463 with Profile/Debug
  binaries ready and `VALIDATE_PHYSICS: ALL PASSED`.
- Fable-05 P3 PhysicsWorld gates:
  Hooke read-only subagent classified all six former throw sites as Lane F
  fixed-capacity/determinism invariants with no caught/recoverable-path risk.
  `python tools\check_runtime_boundaries.py --self-test` passed in
  00:00:00.2918558; `python tools\check_runtime_boundaries.py --max-errors 20`
  passed in 00:00:17.4556095 with 0 errors and
  `MAX_SOURCE_THROW_TOKENS = 275`; `tools\validate_format.bat` passed on the
  final source; `tools\validate_fast.bat` passed in 00:00:45.6318823; and
  `tools\validate_physics.bat` passed in 00:00:26.2402259 with Profile/Debug
  binaries ready and `VALIDATE_PHYSICS: ALL PASSED`.
- Fable-05 P3 GameModelCollection classification:
  P3.3 is documentation-only and required no validation. The classification
  found 8 current throw sites. Pure Lane F candidates are the append topology
  failures at `GameModelCollection.cpp:427`, `:440`, and `:455`; Lane R/result
  or no-op candidates are group descriptor validation `:287`, capacity `:409`,
  missing scene object id `:416`, and legacy camera `GetModelPosition` reads
  `:707`/`:719` after caller cleanup. `Init.cpp:798-805` is the direct
  caught-and-reported append path for `--physics-standalone-smoke`.
- Fable-05 P3 GameModelCollection pure Lane F topology gates:
  `GameModelCollection.cpp:427`, `:440`, and `:455` now use `SB_FATAL`, leaving
  5 intentionally remaining throws for Lane R/API cleanup. `python
  tools\check_runtime_boundaries.py --self-test` passed in 00:00:00.2995922;
  `python tools\check_runtime_boundaries.py --max-errors 20` passed in
  00:00:17.8867793 with 0 errors and `MAX_SOURCE_THROW_TOKENS = 272`;
  `tools\validate_format.bat` passed on the final source;
  `tools\validate_fast.bat` passed in 00:00:39.6990303; and
  `tools\validate_physics.bat` passed in 00:00:15.2978504 with Profile/Debug
  binaries ready and `VALIDATE_PHYSICS: ALL PASSED`.
- Fable-05 P3 GameModelCollection legacy camera pose-read gates:
  `GameModelCollection::GetModelPosition` was replaced with
  `TryGetModelPosition`; absent object-follow camera slots return `false`,
  while present model slots missing a `PhysicsBodyStore` row still fail through
  `SB_FATAL`. `RunRender.cpp` preserves the previous camera target when slot 0
  or 1 is absent. The body-read guardrail now covers `TryGetModelPosition`,
  and `MAX_SOURCE_THROW_TOKENS = 270`. Herschel, a read-only explorer
  subagent, confirmed only the two object-follow camera calls were live callers
  and recommended the no-op/fatal split. Touched-file comment audit inspected
  `GameModelCollection.h`, `GameModelCollection.cpp`, `RunRender.cpp`, and
  `tools/check_runtime_boundaries.py` with 0 deferred. `python
  tools\check_runtime_boundaries.py --self-test` passed in 00:00:00.2679289;
  `python tools\check_runtime_boundaries.py --max-errors 20` passed in
  00:00:17.2286994 with 0 errors; `tools\validate_fast.bat` passed in
  00:00:52.1451648; and `tools\validate_full.bat` passed in
  00:00:45.6008988 with DX12 validation errors 0, screenshots matching
  committed baselines, and `physics_regression_solver.csv` byte-exact.
- Fable-05 P4.2/P4.3 GameModelCollection append Lane R gates:
  `GameModelCollection::AddGameModel` and `AppendGameModelAndPhysicsRows` now
  return `GameModelAppendResult`; invalid group descriptors, capacity exhaustion,
  and missing scene object ids return `SbResult` while topology divergence stays
  `SB_FATAL`. Scene load, generated setup, authored setup, editor placement,
  launcher projectiles, runtime handle smoke, and replay generated-topology
  rebuilds now check status before using the returned body handle.
  `MAX_SOURCE_THROW_TOKENS = 266`, and `tools/check_runtime_boundaries.py` has a
  GameModelCollection no-throw guardrail plus self-tests. Peirce and Halley,
  read-only explorer subagents, checked the fable status/doc targets and
  guardrail placement. Touched-file comment audit inspected 13 source/tool files
  with 0 deferred. `git diff --check`, `tools\validate_format.bat`,
  `python tools\check_runtime_boundaries.py --self-test`, and
  `python tools\check_runtime_boundaries.py --max-errors 20` passed; final
  `tools\validate_full.bat` passed with project filters/runtime boundaries at 0
  errors, Profile/Debug builds at 0 warnings/errors, DX12 validation errors 0,
  screenshots matching baselines, and `physics_regression_solver.csv`
  byte-exact.
- Fable-05 missing-camera parser throw and fable-06 C1 mouse-pickup sub-slice
  gates:
  `TestSceneParser.cpp` now reports missing-camera files through
  `Fail(path, detail)`, lowering `MAX_SOURCE_THROW_TOKENS` from 266 to 265.
  `RunMousePickupState` no longer stores `modelIndex`; mouse pickup stores the
  body handle only, and editor overlay resolves the current row from the body
  store. `MAX_STORED_MODEL_INDEX_MEMBER_FIELDS` dropped from 28 to 27. C1 stays
  open because selection hints and drag-group rows remain. Goodall supplied the
  read-only fable-06 source map and confirmed this as the smallest safe
  sub-slice; Linnaeus supplied the read-only fable-05 missing-camera loader map.
  Touched-file comment audit inspected 7 source-bearing files with 0 deferred.
  `git diff --check` passed; `python tools\check_runtime_boundaries.py
  --self-test` passed; `python tools\check_runtime_boundaries.py --max-errors
  20` passed with 0 errors; `tools\validate_scene_parser_tests.bat` passed in
  00:00:06.4059333; and `tools\validate_fast.bat` passed in 00:00:52.7932666
  with formatting, project filters, staged-size, runtime boundaries, unit
  tests, and Profile/Debug builds all clean.
- Fable-04 L3/L4 gates:
  focused production project-filter validation passed with 0 errors across 2
  production projects; focused Maths and tests project-filter checks passed;
  Profile build passed in 00:00:18.44 and linked `SKULLBONEZ_MATHS.lib` into
  the app and test projects; `tools\validate_tests.bat` passed in
  00:00:01.8538541 with 44/44 doctest cases; `tools\validate_fast.bat` passed
  in 00:00:49.2272698; `tools\validate_full.bat` passed in 00:00:45.8862696
  with DX12 validation errors 0, screenshots matching baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-04 C1 physics timestep header gates:
  `SkullbonezSource/Physics/PhysicsTimestep.h` owns `PHYSICS_FIXED_DT` and
  `PHYSICS_MAX_STEPS_PER_FRAME`; `Common.h` keeps the alias include, and the
  main physics/replay/runtime users include the new header directly. The source
  census returned 12 timestep-token files: the new header plus 11 existing
  users; `RunReplayTools.cpp` owns the two prediction `.inl` users. The first
  `tools\validate_full.bat` failed fast in 00:00:00.9373151 because
  `tools/validate_project_filters.py` did not yet recognize `PhysicsTimestep`;
  after adding the rule, `python tools\validate_project_filters.py --repo .`
  passed in 00:00:00.9141078 with 0 errors across 559 project/filter items.
  Final gates: `tools\validate_fast.bat` passed in 00:00:55.3766968, and
  `tools\validate_full.bat` passed in 00:00:45.2586346 with runtime boundaries
  at 0 errors, Profile/Debug builds at 0 warnings/errors, DX12 validation
  errors 0, screenshots matching baselines, and `physics_regression_solver.csv`
  byte-exact. Touched-file comment audit inspected the 12 C1 source-bearing
  files with 0 deferred.
- Fable-04 C2 scene capacity header gates:
  `SkullbonezSource/GameObjects/SceneCapacity.h` owns
  `TOTAL_CAMERA_COUNT`, `TOTAL_TEXTURE_COUNT`, `DEFAULT_GAME_MODEL_CAPACITY`,
  `MAX_GAME_MODELS`, and `DEFAULT_GAME_MODELS`; `Common.h` keeps the alias
  include. `ActiveGameModelCapacity` and PHYS-012 capacity policy did not move.
  Epicurus, a read-only explorer subagent, confirmed the aggregate header is
  acceptable for C2 as a compile-time capacity home while noting camera/texture
  counts may later deserve narrower owners. `python
  tools\validate_project_filters.py --repo .` passed in 00:00:00.9114220 with
  0 errors across 560 project/filter items; `tools\validate_fast.bat` passed in
  00:01:04.6129410; `tools\validate_full.bat` passed in 00:00:45.5548641 with
  runtime boundaries at 0 errors, Profile/Debug builds at 0 warnings/errors,
  DX12 validation errors 0, screenshots matching committed baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-04 C3 asset key/hash header gates:
  `SkullbonezSource/Assets/AssetKeys.h` owns `HashStr`,
  `TEXTURE_GROUND`, `TEXTURE_BOUNDING_SPHERE`, the sky texture hash constants,
  and `CAMERA_GAME_MODEL_1`/`CAMERA_GAME_MODEL_2`/`CAMERA_FREE`; `Common.h`
  keeps the alias include. Feynman, a read-only explorer subagent, confirmed
  the exact move and warned that `WINDOW_NAME`, `TITLE_TEXT`, and `DATA_ROOT`
  belong to C4. Touched-file comment audit inspected `AssetKeys.h`, `Common.h`,
  and `tools/validate_project_filters.py` with 0 deferred. `python
  tools\validate_project_filters.py --repo .` passed in 00:00:00.9300368 with
  0 errors across 561 project/filter items; `tools\validate_fast.bat` passed in
  00:01:04.6590509; `tools\validate_full.bat` passed in 00:00:45.9861865 with
  runtime boundaries at 0 errors, Profile/Debug builds at 0 warnings/errors,
  DX12 validation errors 0, screenshots matching committed baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-04 C4 window/data-root constants header gates:
  `SkullbonezSource/Runtime/WindowConstants.h` owns `WINDOW_NAME`,
  `TITLE_TEXT`, and `DATA_ROOT`; `Common.h` keeps the alias include.
  Touched-file comment audit inspected `WindowConstants.h`, `Common.h`, and
  `tools/validate_project_filters.py` with 0 deferred. `python
  tools\validate_project_filters.py --repo .` passed in 00:00:00.9377426 with
  0 errors across 562 project/filter items; `tools\validate_fast.bat` passed in
  00:01:04.8565178; `tools\validate_full.bat` passed in 00:00:45.3918458 with
  runtime boundaries at 0 errors, Profile/Debug builds at 0 warnings/errors,
  DX12 validation errors 0, screenshots matching committed baselines, and
  `physics_regression_solver.csv` byte-exact.
- Fable-04 C5 Common.h alias end-state notes:
  `Core/Common.h` now records deletion conditions for `AssetKeys.h`,
  `WindowConstants.h`, `MathsCommon.h`, `SceneCapacity.h`, and
  `PhysicsTimestep.h`, and adds a `Why:` comment tying the remaining
  `Config.h`/`Log.h` compatibility includes to authoritative-plan-02. The diff
  is strictly comments/docs. Touched-file comment audit inspected `Common.h`
  with 0 deferred. No repository validation required.
- Fable-03 P3.0 prediction build-prefix safety prep:
  `RunReplayPredictionState` now owns the build-prefix publication helpers
  (`PublishedBuildFrameCount`, `BuildPrefixShouldBePresented`,
  `BuildFramesAreComplete`, `ResetBuildFramePublication`, and
  `PublishBuildFrameSlot`). External render, automation, Director, scrubber,
  and cause-tree readers no longer touch `buildFrameCount` directly, and
  cancellation/promotion paths document the future async-worker hazard. The
  first `tools\validate_full.bat` attempt failed at formatting for two touched
  files; after formatting only those files, `tools\validate_format.bat` passed
  and the `tools\validate_full.bat` rerun passed in 00:00:51.2659145 with
  project filters/runtime boundaries at 0 errors, Profile/Debug builds at 0
  warnings/errors, DX12 validation errors 0, screenshots matching committed
  baselines, and `physics_regression_solver.csv` byte-exact. Focused
  `prediction_ragdoll_wall_200_predict` proof passed in 00:00:03.9054389 with
  `ok=true`, `predictionPathVisible=true`,
  `liveSolverHashStableAcrossPrediction=true`, and
  `predictionBuildFrameCount=417`.

Logs:

- `Agentic\Reports\2026-07-07\logs\fable-09-prediction-ragdoll-proof-current.log`
- `Agentic\Reports\2026-07-07\logs\fable-09-butterfly-ribbon-proof-current.log`
- `Agentic\Reports\2026-07-07\logs\fable-09-validate-fast.log`
- `Agentic\Reports\2026-07-07\logs\fable-09-validate-dx12-renderer.log`
- `Agentic\Reports\2026-07-07\logs\fable-09-validate-full.log`
- `Agentic\Reports\2026-07-07\logs\fable-09-validate-shaders.log`
- `Agentic\Reports\2026-07-08\logs\fable-09-baseline-divergence-proof.log`
- `Agentic\Reports\2026-07-08\logs\fable-09-prediction-ragdoll-proof-current-rerun.log`
- `Agentic\Reports\2026-07-08\logs\fable-09-baseline-validate-dx12-renderer.log`
- `Agentic\Reports\2026-07-08\logs\fable-09-baseline-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-02-gfx-delete-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-02-gfx-delete-validate-dx12-renderer.log`
- `Agentic\Reports\2026-07-08\logs\fable-02-gfx-delete-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l1-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l2-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l2-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-06-phase1-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-06-phase1-boundary-check.log`
- `Agentic\Reports\2026-07-08\logs\fable-06-r1-r3-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-06-r1-r3-runtime-boundaries.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-memory-overlay-proof.stdout.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-replay-branch-proof.stdout.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-prediction-ragdoll-proof-noguard.stdout.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-forced-probe-failure.stdout.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p2-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l3-l4-profile-build.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l4-validate-tests.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l3-l4-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-l3-l4-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c1-physics-timestep-project-filters.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c1-physics-timestep-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c1-physics-timestep-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c2-scene-capacity-project-filters.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c2-scene-capacity-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c2-scene-capacity-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c3-asset-keys-project-filters.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c3-asset-keys-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c3-asset-keys-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c4-window-constants-project-filters.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c4-window-constants-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-04-c4-window-constants-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-03-p3-0-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-03-p3-0-validate-full-rerun.log`
- `Agentic\Reports\2026-07-08\logs\fable-03-p3-0-prediction-ragdoll-proof.log`
- `Agentic\Reports\2026-07-08\logs\fable-03-p3-0-prediction-ragdoll-proof-rerun.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-spatialgrid-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-spatialgrid-validate-physics.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-physicsworld-boundary-self-test.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-physicsworld-boundary-check.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-physicsworld-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-physicsworld-validate-physics.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-fatal-boundary-self-test.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-fatal-boundary-check.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-fatal-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-fatal-validate-physics.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-camera-boundary-self-test.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-camera-boundary-check.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-camera-validate-fast.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p3-gamemodel-camera-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-append-lane-r-validate-full.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p4-1-scene-tryload-validate-full-final.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-p4-1-scene-parser-tests-final.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-06-scene-parser-tests.log`
- `Agentic\Reports\2026-07-08\logs\fable-05-06-mouse-pickup-validate-fast.log`

## Progress By Markdown File

| File | Done | Pending | Blocked | Total | Progress | Status |
|---|---:|---:|---:|---:|---:|---|
| `01-unit-test-pyramid-progress.md` | 24 | 0 | 0 | 24 | 100.0% | checklist complete |
| `02-global-service-retirement-progress.md` | 14 | 0 | 0 | 14 | 100.0% | checklist complete |
| `03-prediction-isolated-world-progress.md` | 20 | 4 | 0 | 24 | 83.3% | Phase 2/4 complete; Phase 3 safety prep complete; worker-job stepping deferred |
| `04-build-layering-and-repo-hygiene-progress.md` | 13 | 4 | 0 | 17 | 76.5% | phase 2 L1-L4 Maths library extraction and phase 3 C1-C5 domain header splits complete; mega-file decomposition remains |
| `05-unified-error-handling-policy-progress.md` | 18 | 5 | 0 | 23 | 78.3% | P2.1-P2.3 replay probes, P3 SpatialGrid/PhysicsWorld conversions, GameModelCollection classification, pure topology conversion, legacy camera pose-read cleanup, append Lane R cleanup, scene/style TryLoad entry boundary, and direct missing-camera parser throw removal complete; deeper loader/asset and DX12 conversions remain |
| `06-stable-identity-progress.md` | 5 | 9 | 0 | 14 | 35.7% | phase 1 inventory/ratchet, phase 2 central resolvers, and C1 mouse-pickup redundant-row sub-slice complete; selection/drag-group and other subsystem conversions pending |
| `07-blocker-remediation-plan.md` | 0 | 0 | 0 | 0 | n/a | in progress; 24 blockers remain |
| `08-demo-director-progress.md` | 18 | 0 | 0 | 18 | 100.0% | checklist complete |
| `09-consequence-look-progress.md` | 15 | 0 | 0 | 15 | 100.0% | checklist complete |

## Recommended Next Work

1. Continue the earlier unfinished fable queue. The smallest known safe slices
   are fable-06 C1 selection `ModelRowHint` / drag-group cleanup or fable-05
   P4.1 remaining loader/asset Lane R cleanup (the remaining parser throw,
   Terrain, TextureCollection, AssetSystem, and ConvexHullShape).
2. Fable-03 Phase 3 worker-job stepping and fable-07 blocker remediation also
   remain open.

## Process Reminders

- Follow the startup contract in `AGENTS.md` before editing.
- The earlier detached duplicate fable-08 reveal-pacing diff was preserved in
  stash `codex-preserve-detached-reveal-pacing-duplicate-20260708`; do not drop
  it unless the user asks.
- Documentation-only changes require no repository validation.
- Source-bearing changes require the touched-file comment-quality audit.
- Commit and push normal feature-branch commits on
  `nightrunner-7th-july-complete-fables`.
