# Fable Plans Handoff - 2026-07-07

## User Direction

Continue from `main` on a new night-runner branch without throwing away existing
work. The user first asked to work through fable plans 1-7 only, then approved
continuing into fable-08 and fable-09. For fable-09, the user specifically
wants debug lines to become smooth/anti-aliased rather than jaggy, with shader
options for glow/emphasis in the butterfly-effect demo.

## Current Branch

- Branch: `nightrunner-7th-july-plans-1-7`
- Repo: `C:\SkullbonezCore`
- Branch base: `main` at worktree creation.
- Worktree state before the latest source slice: clean on the new branch.
- CodeGraph exists in this repo; run `codegraph status .` before code
  exploration when continuing source work.

## Current Clean Exit Point

Fable-08 P3.2 phase-entry reveal-rate control is complete and validated.

Latest validated slice:

- Fable-08 P3.2 phase-entry reveal-rate control.

What it did:

- Promotes replay prediction reveal pacing from the old fixed
  `REPLAY_PREDICTION_REVEAL_SECONDS_PER_SECOND` constant to
  `RunReplayPredictionState::revealSecondsPerSecond`, defaulting to 1.0.
- Lets `DemoDirectorPlayback::Tick(...)` apply the active phase `revealRate`
  once per phase entry, preserving the current reveal cursor by re-anchoring
  already revealed prediction seconds.
- Adds interaction report fields for `directorPhaseRevealRate`,
  `directorAppliedRevealRate*`, and `predictionRevealSecondsPerSecond`.
- Records the fable-09 smooth/anti-aliased debug-line plus shader glow option
  requirement in the plan/progress files.
- Completes the touched-file comment audit for the fable-08 source-bearing
  files: 8 inspected, 0 deferred.

Validation/evidence for the latest slice:

- Director reveal-rate proof passed with report `ok=true`,
  `directorAppliedRevealRate=0.25`,
  `directorAppliedRevealRateCount=1`, and
  `predictionRevealSecondsPerSecond=0.25`.
- `prediction_ragdoll_wall_200_predict` proof passed with report `ok=true`,
  `predictionPathVisible=true`, `liveSolverHashStableAcrossPrediction=true`,
  `predictionRevealSecondsPerSecond=1.0`, and allocation guard
  `gameplay_violations=0`.
- `tools\validate_fast.bat` passed:
  - source formatting, project filters, staged-size, runtime boundaries, unit
    tests, and Profile/Debug builds passed.
  - Profile and Debug builds had 0 warnings/errors.
- `tools\validate_dx12_renderer.bat` passed:
  - DX12 InfoQueue reported 0 validation errors.
  - DX12 screenshots matched committed baselines.
- `tools\validate_full.bat` passed:
  - runtime boundaries passed.
  - Profile and Debug builds had 0 warnings/errors.
  - DX12 InfoQueue reported 0 validation errors.
  - DX12 screenshots matched committed baselines.
  - `physics_regression_solver.csv` matched the committed baseline byte-exactly.

Logs:

- `Agentic\Reports\2026-07-07\logs\fable-08-p3-2-reveal-rate-interaction.log`
- `Agentic\Reports\2026-07-07\logs\fable-08-p3-2-prediction-ragdoll-proof.log`
- `Agentic\Reports\2026-07-07\logs\fable-08-p3-2-validate-fast.log`
- `Agentic\Reports\2026-07-07\logs\fable-08-p3-2-validate-dx12-renderer.log`
- `Agentic\Reports\2026-07-07\logs\fable-08-p3-2-validate-full.log`

Previous read-only subagents:

- Subagent `019f3bf3-2abd-78e1-a123-4930a99c2a24` / `Euler` reviewed the
  Profiler renderer-diagnostics checkpoint read-only and found no blocking
  issues. It specifically checked bind/clear/backend teardown order, Core
  profiler global removal, checker ratchets, validation evidence, and that
  fable-08/fable-09 stayed untouched.
- Subagent `019f3c18-e1e8-7f12-9c11-9334c8333d36` / `Leibniz` mapped the
  remaining fable-02 renderer-service callers read-only. It confirmed the
  PhysicsDebug in-flight shape, named `CollisionVisualizer.cpp` as the
  remaining direct physics-debug renderer-service caller, and recommended the
  `Window::SetResizeRenderBackend` `IRenderDeviceLifecycle` narrowing as the
  smallest next slice if the goal is narrow aggregate-backend debt.
- Subagent `019f3c22-a2d9-7c50-8b82-ab8721a6e778` / `Socrates` mapped the
  `CollisionVisualizer.cpp` renderer-global cleanup read-only. Its map was
  implemented in the latest slice: resource creation/destruction now uses
  `IRenderResourceFactory`, draw/state calls use `IRenderCommandContext`, and
  visualizer resource teardown runs while the resource factory is still live.
- Subagent `019f3c2c-4a6e-7c12-a9a2-e468b1345113` / `Carver` reviewed the
  docs/checker sidecar read-only and confirmed the expected ratchets:
  `MAX_GLOBAL_SERVICE_ACCESS_CENSUS = 92`,
  `MAX_IRENDER_BACKEND_DEPENDENCY_CENSUS = 30`, and no CollisionVisualizer
  renderer-service/`IRenderBackend` rows.
- Subagent `019f3c2f-b2ea-7841-84d4-f9d3b361cfc9` / `Carson` reviewed the dirty
  CollisionVisualizer diff read-only. It found the stale compatibility helper
  call site and the stale PHYS-034 `GameModelCollection` ratchet slack; both
  were fixed before validation.
- Subagent `019f3c3d-9fd0-7170-b1d2-afbe946ec7bf` / `Franklin` mapped the
  draw-call trace helper migration read-only. It recommended moving trace
  scope ownership to `IRenderDiagnostics.h`, adding diagnostics to
  `RenderHelperContext`/`UIRenderContext`, updating trace macro call sites, and
  tightening the aggregate-backend checker rows; the latest slice implements
  that map.
- Subagent `019f3c59-258c-7c90-8031-8a3a7f6bec83` / `Aquinas` mapped the
  detached fable-08 dirty worktree read-only. It identified the in-flight diff
  as P3.2 reveal-rate wiring and warned that only the two
  `RunFrame.cpp` prediction arguments should be hand-ported onto the current
  branch. Its dry patch check briefly dirtied the clean worktree and restored
  the affected files; the final P3.2 implementation was re-applied and
  validated afterward.
- Subagent `019f3c59-4b47-78c2-82e6-780b258c704b` / `Confucius` mapped
  fable-09 debug-line rendering read-only. It found the current path is a
  1px DX12 line-list shader (`grid_line.hlsl`) with no edge-distance signal,
  so true anti-aliasing should use a separate styled/ribbon line path for
  replay causal overlays rather than changing every generic debug line.

## Progress By Markdown File

Checkbox counts are mechanical counts of `- [x]`, `- [ ]`, and `- [B]` rows in
each markdown file at the time of this handoff.

| File | Done | Pending | Blocked | Total | Progress | Status |
|---|---:|---:|---:|---:|---:|---|
| `01-unit-test-pyramid-plan.md` | 0 | 0 | 0 | 0 | n/a | Implemented 2026-07-07 |
| `01-unit-test-pyramid-progress.md` | 24 | 0 | 0 | 24 | 100.0% | in progress, checklist complete |
| `02-global-service-retirement-plan.md` | 0 | 0 | 0 | 0 | n/a | draw-call trace diagnostics-facet cleanup complete; Gfx accessor/facade deletion pending |
| `02-global-service-retirement-progress.md` | 10 | 4 | 0 | 14 | 71.4% | draw-call trace diagnostics-facet cleanup complete; Gfx accessor/facade deletion pending |
| `03-prediction-isolated-world-plan.md` | 0 | 0 | 0 | 0 | n/a | Phase 2 and 4 complete; Phase 3 worker-job stepping deferred |
| `03-prediction-isolated-world-progress.md` | 19 | 4 | 0 | 23 | 82.6% | in progress |
| `04-build-layering-and-repo-hygiene-plan.md` | 0 | 0 | 0 | 0 | n/a | Phase 1 repo hygiene complete; library layering not started |
| `04-build-layering-and-repo-hygiene-progress.md` | 4 | 13 | 0 | 17 | 23.5% | phase 1 complete; phase 2 not started |
| `05-unified-error-handling-policy-plan.md` | 0 | 0 | 0 | 0 | n/a | Phase 1 policy and ratchet complete; conversions not started |
| `05-unified-error-handling-policy-progress.md` | 4 | 13 | 0 | 17 | 23.5% | phase 1 complete; conversions not started |
| `06-stable-identity-plan.md` | 0 | 0 | 0 | 0 | n/a | Proposed |
| `06-stable-identity-progress.md` | 0 | 14 | 0 | 14 | 0.0% | not started |
| `07-blocker-remediation-plan.md` | 0 | 0 | 0 | 0 | n/a | In progress; 26 blockers remain |
| `08-demo-director-plan.md` | 0 | 0 | 0 | 0 | n/a | Phase 3 P3.2 reveal-rate control complete |
| `08-demo-director-progress.md` | 13 | 5 | 0 | 18 | 72.2% | P3.2 complete; Phase 4 next |
| `09-consequence-look-plan.md` | 0 | 0 | 0 | 0 | n/a | Proposed; smooth AA/glow line requirement recorded |
| `09-consequence-look-progress.md` | 0 | 15 | 0 | 15 | 0.0% | not started; smooth AA/glow line requirement recorded |
| `HANDOFF-2026-07-07.md` | 0 | 0 | 0 | 0 | n/a | this handoff |
| `README.md` | 0 | 0 | 0 | 0 | n/a | index only |

## Recommended Next Work

1. Continue fable-08 Phase 4 next: implement advance rules P4.1, then
   automation verbs P4.2, then author `SkullbonezData/shots/butterfly.shot.json`
   for P4.3.
2. After fable-08 is committed through Phase 4/closure, start fable-09
   discovery. Confucius' read-only map says the line-rendering work should
   create a separate styled/ribbon debug-line path for replay causal overlays,
   with shader-controlled anti-alias feather and optional glow/emphasis.
3. Fable-02 Gfx accessor/facade deletion remains open but is no longer the
   current requested queue.

## Remaining High-Level Work

- Fable-08: Phase 4 and Closure.
- Fable-02: Gfx accessor/facade deletion remains after the Profiler
  renderer-diagnostics bind, RunPasses readiness probe, and RunInput stale
  allowance cleanup, capture-facade cleanup, Broadphase visualizer cleanup, and
  PhysicsDebug visualizer cleanup, Window resize lifecycle cleanup, and
  CollisionVisualizer plus draw-call trace diagnostics-facet cleanups.
- Fable-03: remaining worker-job stepping/deferred isolation work.
- Fable-04: library layering phases after repo hygiene.
- Fable-05: throw-to-policy conversions after the ratchet.
- Fable-06 not started.
- Fable-09: not started; user requirement for smooth/anti-aliased debug lines
  plus shader glow/emphasis controls is recorded.
- `Agentic/Plans/In_Progress/overnight-blockers-2026-07-07.md` still tracks 26
  blocked rows.

## Process Reminders

- Follow the startup contract in `AGENTS.md` before editing.
- Protect dirty worktrees; do not revert user-owned work.
- Implementing repository changes from plans should use
  `Agentic/Skills/orchestrator/SKILL.md` unless the user asks to bypass it.
- Documentation-only changes require no repository validation.
- Source-bearing changes require the touched-file comment-quality audit.
- Commit and push on `nightrunner-7th-july-plans-1-7`.
