# Engine Cleanup Handoff - 2026-07-08

## Current state

- Branch: `nightrunner-8th-july`
- Remote: pushed through `91e220fa cleanup(06): delete replay prediction inl splices`
- Worktree before this handoff doc: clean
- CodeGraph: synced after the last pushed code slice
- Goal start time recorded by agent: `2026-07-08T17:09:45.2555600+10:00`
- Handoff written: `2026-07-09T08:14:08+10:00`

## What changed in this run

Pushed cleanup commits in order:

- `6104e8f4 docs(01): close run decomposition plan`
- `a9cf2c38 docs: update engine cleanup ledger`
- `7dd97478 cleanup(09): split prediction reveal clock`
- `d4a8d4b1 cleanup(09): split prediction ui state`
- `0425ad07 cleanup(09): split prediction future-node cache`
- `7d960136 cleanup(09): split prediction build state`
- `40b6c8f7 cleanup(09): split prediction simulation state`
- `7b5f8b6c cleanup(09): template replay future helpers`
- `fc45cba4 cleanup(09): move replay probes out of RunFrame`
- `3dcdbd92 cleanup(06): promote replay import/export TU`
- `b791fef5 cleanup(06): promote replay query tools TU`
- `97cc0485 cleanup(06): promote replay cause tree TU`
- `4fb0bfd1 cleanup(06): promote replay scrubber TU`
- `91e220fa cleanup(06): delete replay prediction inl splices`

Plan closures now recorded in `engine-cleanup-plans`:

- Plan 06 is `Complete`.
- Plan 09 is `Complete`.
- README status table now marks plans 06 and 09 as `Complete`.

Structural cleanup evidence:

- `rg -n '#include ".*\.inl"' SkullbonezSource\Runtime` reports no matches.
- `git ls-files SkullbonezSource\Runtime | rg '\.inl$'` reports no tracked runtime `.inl` files.
- Active source/project/tool residue scan reports no references to `RunReplayPredictionHelpers.inl` or `RunReplayPredictionVisualizer.inl`.
- `RunReplayTools.cpp` is now a real 3,699-line TU instead of a shell assembled from remaining prediction `.inl` bodies.

## Validation evidence

Latest code slice (`91e220fa`):

- `tools\validate_build.bat Profile`: passed in 7.3s, 0 warnings/errors.
- `tools\validate_project_filters.bat`: passed in 1.2s.
- `python tools\check_runtime_boundaries.py --repo .`: passed in 18.2s, 0 errors.
- `python tools\check_runtime_boundaries.py --self-test`: passed in 0.3s.
- `python -m py_compile tools\check_runtime_boundaries.py`: passed in 0.2s.
- `tools\validate_format.bat`: passed in 9.6s after narrow formatting.
- `tools\validate_replay_scrub.bat`: passed in 22.9s with Debug/Profile builds at 0 warnings/errors.
- `tools\validate_full.bat`: passed in 45.2s with project filters/runtime boundaries clean, 0 build warnings/errors, 0 DX12 validation errors, matching DX12 screenshots, and `physics_regression_solver.csv` byte-exact at 20001 lines.

Butterfly proof:

```bat
Profile\SKULLBONEZ_CORE.exe --scene SkullbonezData\scenes\prediction_ragdoll_wall_200.scene.json --interaction-script Agentic\Temp\butterfly_shot_take_interaction.json --interaction-report TestOutput\interaction\engine_cleanup_butterfly_shot_take_report.json --frames 430 --replay on --replay-seconds 2 --fixed-step --vsync off
```

Result: passed in 7.4s with `ok=true`, reached `root-sighting`, `chain-bloom`,
and `settle-rest`, kept `predictionPathVisible=true`, kept
`liveSolverHashStableAcrossPrediction=true`, and saved the three Butterfly phase
screenshots.

Log files are under `TestOutput\agent_logs\prediction_foldback_*` and
`TestOutput\agent_logs\scrubber_promotion_*`.

## Comment audit

Touched source/tool files audited against
`Agentic/Reference/comment-style-guide.md`:

- `SkullbonezSource/Runtime/Replay/RunReplayTools.cpp`
- `SkullbonezSource/Runtime/Replay/RunReplayScrubberTools.cpp`
- `SkullbonezSource/Runtime/Replay/ReplayInteractionController.h`
- `SkullbonezSource/Runtime/Replay/RunReplayImportExport.h`
- `tools/check_runtime_boundaries.py`

No deferred source-comment work remains from the completed slices.

## Remaining folder status

Complete or done:

- 01 Run god-object decomposition
- 06 `.inl` translation-unit un-splitting
- 08 RenderHelper global-state removal
- 09 Replay subsystem right-sizing
- 10 EngineContext / IRenderBackend boundary
- 12 Ambient singletons: Log / Profiler

Open/proposed or gated:

- 02 PhysicsWorld solver decomposition: large remaining implementation after phase 0.
- 03 Governance apparatus removal: explicit owner sign-off required before deleting checker/contracts.
- 04 Error-handling policy reconciliation: large remaining throw-lane implementation.
- 05 Behavioral test coverage: large remaining test program.
- 07 Allocation-gate right-sizing: explicit DECIDE gate before changing policy.
- 11 Render abstraction leaks: in progress, but phase 0 has a DECIDE gate.
- 13 Facade retirement: in progress; FAC-005 needs a human-owned physics API plan.

## Resume notes

1. Run the normal startup contract from `AGENTS.md`.
2. Use `Agentic/Skills/orchestrator/SKILL.md` before implementing more plan work.
3. Use CodeGraph first for source exploration; `.codegraph/` exists and was synced.
4. Run `git status --short --branch` before edits; the branch should be clean except this handoff doc if it has not been committed yet.
5. Do not run broad validation while iterating. For PR-bound Runtime changes, the prior successful gate was `tools\validate_full.bat`; replay changes also used `tools\validate_replay_scrub.bat`.
