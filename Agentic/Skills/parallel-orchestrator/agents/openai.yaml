interface:
  display_name: "Parallel Orchestrator"
  short_description: "Parallelize plans and safe bug fixes"
  default_prompt: "Use $parallel-orchestrator to execute the active plan with dependency-safe sub-agents, worktrees, and mergeable bug fixes."
