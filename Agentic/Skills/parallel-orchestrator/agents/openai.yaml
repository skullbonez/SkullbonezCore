interface:
  display_name: "Parallel Orchestrator"
  short_description: "Run independent plans concurrently"
  default_prompt: "Use $parallel-orchestrator to run every dependency-ready plan and safe bug fix concurrently in isolated worktrees, then integrate them through the main orchestrator."
